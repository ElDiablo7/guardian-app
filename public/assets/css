    :root {
        --bg0: #05070d;
        --bg1: #070b16;
        --text: #e7eefc;
        --muted: #c1c8e4;
        --cyan: #53e6ff;
        --amber: #ffd27a;
        --red: #ff4c4c;
        --green: #48ffb0;
        --card: rgba(11, 16, 34, .72);
        --border: rgba(63, 93, 182, .40);
        --mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
        --sans: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
    }

    body {
        margin: 0;
        padding: 0;
        font-family: var(--sans);
        color: var(--text);
        background: linear-gradient(180deg, var(--bg0), var(--bg1));
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .fx-bg {
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: -1;
        background: repeating-linear-gradient(180deg, rgba(72, 255, 176, .03) 0, rgba(72, 255, 176, .03) 1px, transparent 2px, transparent 10px);
        transform: rotate(2deg) scale(1.1);
        opacity: .25;
    }

    .fx-bg--hard {
        background: repeating-linear-gradient(180deg, rgba(83, 230, 255, .04) 0, rgba(83, 230, 255, .04) 1px, transparent 2px, transparent 10px);
    }

    .topbar {
        width: min(1000px, 92vw);
        margin: 38px auto 0;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 18px;
        padding: 14px 18px;
        border: 1px solid var(--border);
        background: var(--card);
        border-radius: 20px;
        box-shadow: 0 8px 22px rgba(0, 0, 0, .45);
        backdrop-filter: blur(10px);
    }

    .brand {
        display: flex;
        align-items: center;
        gap: 14px;
    }

    .brand h1 {
        margin: 0;
        font-size: 15px;
        letter-spacing: .2em;
        text-transform: uppercase;
        color: var(--green);
    }

    .brand .sub {
        font-family: var(--mono);
        font-size: 11px;
        color: var(--muted);
        letter-spacing: .08em;
        margin-top: 3px;
    }

    .mark {
        width: 40px;
        height: 40px;
        border-radius: 12px;
        border: 1px solid rgba(72, 255, 176, .5);
        background: radial-gradient(60% 60% at 30% 25%, rgba(72, 255, 176, .4), transparent 60%),
            linear-gradient(180deg, rgba(75, 121, 255, .2), rgba(11, 16, 34, .8));
    }

    .status {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        align-items: center;
    }

    .pill {
        font-family: var(--mono);
        font-size: 11px;
        padding: 6px 10px;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: rgba(11, 16, 34, .55);
        color: var(--muted);
        letter-spacing: .06em;
    }

    .pill.live {
        border-color: rgba(72, 255, 176, .5);
        color: var(--green);
    }

    .container {
        width: min(1000px, 92vw);
        margin: 40px auto;
    }

    .hero {
        text-align: center;
        padding: 40px 20px;
        border-radius: 24px;
        background: linear-gradient(180deg, rgba(11, 16, 34, .7), rgba(5, 7, 13, .8));
        border: 1px solid rgba(72, 255, 176, .3);
        box-shadow: 0 10px 40px rgba(0, 0, 0, .5);
    }

    .hero h2 {
        font-size: 28px;
        margin: 0 0 16px;
        color: var(--green);
        font-weight: 500;
    }

    .hero .lead {
        font-size: 16px;
        max-width: 70ch;
        margin: 0 auto 30px;
        line-height: 1.6;
        color: var(--text);
    }

    .pricing-card {
        max-width: 500px;
        margin: 0 auto;
        background: rgba(11, 16, 34, .9);
        border: 1px solid var(--cyan);
        border-radius: 20px;
        padding: 30px;
        text-align: left;
    }

    .price-head {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        border-bottom: 1px solid var(--border);
        padding-bottom: 16px;
        margin-bottom: 20px;
    }

    .price-head h3 {
        margin: 0;
        font-size: 18px;
        color: var(--cyan);
        text-transform: uppercase;
        letter-spacing: .05em;
    }

    .price {
        font-size: 32px;
        font-weight: 700;
        color: #fff;
    }

    .price small {
        font-size: 14px;
        color: var(--muted);
        font-weight: 400;
    }

    .features {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .features li {
        padding: 10px 0;
        border-bottom: 1px dashed rgba(255, 255, 255, .1);
        font-size: 14px;
        line-height: 1.5;
        color: var(--muted);
    }

    .features li:last-child {
        border: none;
    }

    .features strong {
        color: var(--text);
    }

    .btn {
        appearance: none;
        border: 1px solid rgba(63, 93, 182, .55);
        background: rgba(11, 16, 34, .6);
        color: var(--text);
        border-radius: 999px;
        padding: 10px 16px;
        font-family: var(--mono);
        font-size: 12px;
        letter-spacing: .07em;
        cursor: pointer;
        text-decoration: none;
        transition: all .2s;
        display: inline-block;
    }

    .btn.primary {
        border-color: rgba(83, 230, 255, .7);
        background: rgba(83, 230, 255, .1);
        color: var(--cyan);
    }

    .btn.warning {
        border-color: rgba(255, 210, 122, .7);
        background: rgba(255, 210, 122, .1);
        color: var(--amber);
    }

    .btn.btn-large {
        font-size: 14px;
        padding: 14px 24px;
        width: 100%;
        border-radius: 12px;
    }

    .btn:hover {
        filter: brightness(1.2);
        transform: translateY(-1px);
    }

    .fine-print {
        font-family: var(--mono);
        font-size: 11px;
        color: rgba(255, 255, 255, .4);
        margin-top: 12px;
    }

    .grid {
        display: grid;
        gap: 20px;
    }

    .two-col {
        grid-template-columns: 1fr 1fr;
    }

    .card {
        background: var(--card);
        border: 1px solid var(--border);
        padding: 24px;
        border-radius: 20px;
    }

    .card h3 {
        margin: 0 0 12px;
        font-size: 16px;
        color: var(--cyan);
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .card p {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
        line-height: 1.6;
    }

    .dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        display: inline-block;
    }

    .dot.grn {
        background: var(--green);
        box-shadow: 0 0 10px var(--green);
    }

    .dot.amb {
        background: var(--amber);
        box-shadow: 0 0 10px var(--amber);
    }

    .app-card {
        border-color: rgba(83, 230, 255, .4);
        box-shadow: 0 10px 40px rgba(0, 0, 0, .5);
    }

    .app-card h2 {
        margin: 0 0 8px;
        color: var(--cyan);
        text-transform: uppercase;
        font-size: 20px;
    }

    textarea {
        width: 100%;
        height: 140px;
        background: rgba(5, 7, 13, .8);
        border: 1px solid var(--cyan);
        border-radius: 12px;
        padding: 16px;
        color: var(--text);
        font-family: var(--mono);
        font-size: 13px;
        resize: vertical;
        margin-bottom: 16px;
        box-sizing: border-box;
        box-shadow: inset 0 2px 10px rgba(0, 0, 0, .5);
    }

    textarea:focus {
        outline: none;
        border-color: var(--green);
        box-shadow: 0 0 0 2px rgba(72, 255, 176, .3);
    }

    #results {
        display: none;
        margin-top: 24px;
        padding-top: 24px;
        border-top: 1px solid var(--border);
    }

    .threat-item {
        padding: 14px 18px;
        border-radius: 12px;
        margin-bottom: 12px;
        font-size: 14px;
        font-family: var(--sans);
        line-height: 1.5;
    }

    .threat-head {
        font-family: var(--mono);
        font-size: 12px;
        margin-bottom: 6px;
        font-weight: 700;
    }

    .threat-critical {
        background: rgba(255, 76, 76, .15);
        border: 1px solid rgba(255, 76, 76, .4);
        color: #fff;
        border-left: 4px solid var(--red);
    }

    .threat-critical .threat-head {
        color: #ff8888;
    }

    .threat-warn {
        background: rgba(255, 210, 122, .15);
        border: 1px solid rgba(255, 210, 122, .4);
        color: #fff;
        border-left: 4px solid var(--amber);
    }

    .threat-warn .threat-head {
        color: var(--amber);
    }

    .threat-safe {
        background: rgba(72, 255, 176, .1);
        border: 1px solid rgba(72, 255, 176, .3);
        color: var(--muted);
        border-left: 4px solid var(--green);
    }

    .threat-safe .threat-head {
        color: var(--green);
    }

    .actions-box {
        background: rgba(5, 7, 13, .8);
        padding: 20px;
        border-radius: 12px;
        border: 1px dashed var(--border);
        margin-top: 20px;
    }

    .actions-box h4 {
        margin: 0 0 12px;
        color: var(--cyan);
        font-family: var(--mono);
        text-transform: uppercase;
    }

    .actions-box ul {
        margin: 0;
        padding-left: 20px;
        color: var(--text);
        font-size: 14px;
        line-height: 1.6;
    }

    .actions-box li {
        margin-bottom: 8px;
    }

    .actions-box a {
        color: var(--cyan);
    }

    footer {
        width: min(1000px, 92vw);
        margin: 40px auto;
        display: flex;
        justify-content: space-between;
        font-family: var(--mono);
        font-size: 11px;
        color: var(--muted);
        border-top: 1px solid var(--border);
        padding-top: 20px;
    }